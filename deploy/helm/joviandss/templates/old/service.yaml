apiVersion: v1
kind: Service
metadata:
  name: {{ include "joviandss.fullname" . }}
  labels:
    {{- include "joviandss.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "joviandss.selectorLabels" . | nindent 4 }}
