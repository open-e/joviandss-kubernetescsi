apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: snapshot-test
spec:
  volumeSnapshotClassName: joviandss-snapshot-sc
  source:
    persistentVolumeClaimName: pv-test-pvc
